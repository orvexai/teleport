---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: teleport-dev-workspace
  namespace: teleport-dev
spec:
  accessModes: ["ReadWriteMany"]
  storageClassName: rook-cephfs
  resources:
    requests:
      storage: 40Gi
